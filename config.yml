http_server:
  port: 8080

auth:
  access_expire: 24h
  refresh_expire: 168h

mysql:
  db_name: gameapp_db
  host: localhost
  port: 3308

matching:
  waiting_timeout: 2m

redis:
  host: localhost
  port: 6380
  db: 0

presence:
  expire: "60m"
  prefix: "presence"

scheduler:
  interval_second: 30
 
